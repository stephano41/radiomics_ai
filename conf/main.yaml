defaults:
  - pipelines: main_pipeline
  - experiments: meningioma
  - hydra/run/dir: job_timestamp
  - _self_
  - optional local_env: env
  - override hydra/job_logging: custom

output_root: ./outputs
n_cpu: 4



