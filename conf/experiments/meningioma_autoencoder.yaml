# @package _global_

defaults:
    - meningioma.yaml
    - /autoencoder: resnetsegvae
    - _self_

name: meningioma+autoencoder

feature_dataset:
     additional_features: ['ID']

analysis:
    image_modalities: ['registered_adc', 't2', 'flair', 't1', 't1ce', 'SegResNet']
